/// `zig` 命令介绍
///     - build：构建项目，自动搜索当前目录以及父目录的 `build.zig` 进行构建。
///     - build-obj: 将zig源文件编译成一个目标文件（xxx.o）。
///     - build-test：编译并执行 Zig 文件中的所有测试用例。
///     - init：用于初始化一个项目，在当前路径下创建 `src/main.zig`、`src/root.zig`、`build.zig`和 `build.zig.zon` 四个文件。
///     - run: 编译并立即运行一个Zig程序。
///     - cc: 使用 Zig 内置的 C编译器来编译C源文件。
///     - c++：使用 Zig 内置的 C++ 编译器来编译C++源文件。
///     - translate-c: 将C代码转换为 Zig代码。
///     - targets：显示 Zig 编译器支持的所有目标架构、操作系统和 ABI。
///
pub fn main() void {}
